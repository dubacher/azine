Tips&Tricks:

#Setup user for repo
$ git config user.name "Marco Dubacher"
$ git config user.email "github@dubacher.com"

#Django Scaffolding
django-admin-2.5.py startproject [site-name]
python [site-name]/manage.py startapp [appname]
python manage.py syncdb

#Show sql of appname
python manage.py sql [appname]

#Start built-in webserver
python manage.py runserver <port>

Install Django:
- Die Django-Installation mit MacPorts installiert soweit ich gesehen habe Django-1.0.0. Dieses unterstützt z.B. csrf nicht...
  -> Eigene Django installation und ln -s /Users/marco/Documents/workspaces/libs/Django-1.2.1/django django

Installation South
- South herunterladen 
- Symlink in /opt/local/lib/python2.5/site-packages e.g. ln -s /Users/marco/Documents/workspaces/libs/south/south south

Cron-Job-Library for Django:
- http://code.google.com/p/django-cron/

Create and Load fixtures:
Dump all data of an APP: manage.py dumpdata --indent=2 app-name  > ./app-name/fixtures/bootstrap.json
Load all data of an APP: manage.py loaddata bootstrap.json

Dump data of cms: manage.py dumpdata cms > ./cms/fixtures/bootstrap.json
Load data of cms: manage.py loaddata bootstrap.json

(For cms.plugin.text it is "manage.py dumpdate cms text > ./cms/fixtures/bootstrap.json)

Database Migrations:
--------------------
For the one who gets a migration:
1. Possibility
- Migrate with south
2. Possibility
> ./manage.py flush
> ./manage.py syncdb
> ./manage.py loaddata bootstrap

For the one who 
1. Possiblity
- Migrate data using south
- If tables are changed which have an impact on the fixture then perform a datadump
> ./manage.py dumpdata --indent=2  cms cms text > ./cms/fixtures/bootstrap.json
